{
  "name": "exhibition-curator",
  "version": "1.0.0",
  "description": "Exhibition Curator - Virtual Museum Platform",
  "scripts": {
    "build": "cd be && npm install && npm run build",
    "start": "cd be && npm start",
    "dev": "cd be && npm run dev",
    "build:frontend": "cd curator-fe && npm install && npm run build",
    "start:frontend": "cd curator-fe && npm start"
  },
  "workspaces": [
    "be",
    "curator-fe"
  ],
  "engines": {
    "node": ">=18.0.0"
  }
}